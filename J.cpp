#include<bits/stdc++.h>
using namespace std;

#define optimize() ios_base:: sync_with_stdio(0); cin.tie(0); cout.tie(0);

int main() {
    int x, y;
    cin >> x >> y;
    cout << ((x + y) * 10) + 1 << endl;
    return 0;
}